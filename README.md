# Windows
python -m venv venv

venv\Scripts\activate

# устанавливаем Django
pip install django

# библиотеки для работы с Excel и изображениями
pip install openpyxl
pip install Pillow

# выводим список доступных команд Django
python manage.py help

# используем все импорты (orders последний )